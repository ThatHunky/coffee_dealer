# User Management Feature - Редагування та Видалення Користувачів

## 📋 Огляд

Додано функціонал для керування користувачами з можливістю редагування та деактивації/активації користувачів. Адміністратори тепер можуть легко оновлювати інформацію про користувачів або тимчасово деактивувати їх без втрати історичних даних.

## ✨ Нові Можливості

### 1. **Редагування Користувача** (`/edituser`)

Інтерактивна команда для зміни параметрів існуючого користувача.

**Формат:**
```
/edituser <позиція|ім'я> [нове_ім'я_укр] [нове_ім'я_англ] [новий_колір]
```

**Особливості:**
- Можна знайти користувача за позицією (0-7) або ім'ям
- Використовуйте `-` для параметрів, які не потрібно змінювати
- Валідація кольору (має бути у форматі #RRGGBB)
- Зберігає статус активності користувача

**Приклади:**
```bash
# Змінити всі параметри користувача на позиції 0
/edituser 0 Діана diana #4A90E2

# Змінити тільки колір користувача diana
/edituser diana - - #FF5733

# Змінити ім'я, залишити колір
/edituser 0 Діанка dianochka -
```

### 2. **Деактивація Користувача** (`/removeuser`)

Деактивує користувача без видалення історичних даних.

**Формат:**
```
/removeuser <позиція|ім'я>
```

**Особливості:**
- Користувач не видаляється з бази даних
- Історія призначень зберігається
- Користувач більше не відображається в активних розкладах
- Можна повторно активувати пізніше

**Приклади:**
```bash
# Деактивувати користувача за позицією
/removeuser 0

# Деактивувати користувача за ім'ям
/removeuser diana
```

**Повідомлення:**
```
✅ Користувача деактивовано:
Позиція: 0
Ім'я: Діана (diana)

⚠️ Користувач більше не буде відображатися в розкладі.
Для повторної активації використовуйте /edituser або /adduser.
```

### 3. **Активація Користувача** (`/activateuser`)

Повертає раніше деактивованого користувача до активного стану.

**Формат:**
```
/activateuser <позиція|ім'я>
```

**Особливості:**
- Відновлює деактивованого користувача
- Користувач знову з'являється в розкладах
- Зберігає всі налаштування (ім'я, колір)

**Приклади:**
```bash
# Активувати користувача за позицією
/activateuser 0

# Активувати користувача за ім'ям
/activateuser diana
```

### 4. **Оновлена Команда `/users`**

Тепер показує статус активності кожного користувача.

**Приклад виводу:**
```
👥 Список користувачів:

✅ Позиція 0: Діана (diana)
   🎨 Колір: #4A90E2

✅ Позиція 1: Дана (dana)
   🎨 Колір: #9B59B6

❌ Позиція 2: Женя (zhenya)
   🎨 Колір: #2ECC71
```

- ✅ = Активний користувач
- ❌ = Деактивований користувач

## 🔧 Технічна Реалізація

### Зміни в `bot.py`

#### Нові Обробники Команд

1. **`cmd_edit_user`** (line ~910-980)
   - Знаходить користувача за позицією або ім'ям
   - Підтримує часткове оновлення (тире для пропуску параметрів)
   - Валідує колір формату hex
   - Логує всі операції редагування

2. **`cmd_remove_user`** (line ~983-1043)
   - Перевіряє існування користувача
   - Встановлює `is_active=False`
   - Зберігає всі дані користувача
   - Надає інструкції для повторної активації

3. **`cmd_activate_user`** (line ~1046-1107)
   - Перевіряє чи користувач деактивований
   - Встановлює `is_active=True`
   - Відновлює користувача в розкладах

#### Оновлені Розділи

1. **Довідка `/help`** (line ~226-228)
   - Додано три нові команди до списку адміністраторських команд

2. **Bot Commands** (line ~1493-1496)
   - Додано hint для нових команд в меню бота

### Взаємодія з User Manager

Всі команди використовують метод `user_manager.update_user()` для:
- Збереження змін у базі даних
- Оновлення кешу
- Синхронізації конфігурації

### Логування

Кожна операція логується з деталями:
```python
logger.info(f"Admin {message.from_user.id} editing user: {user.name_en}")
logger.error(f"Error in cmd_edit_user: {e}", exc_info=True)
```

## 📝 Використання

### Сценарій 1: Зміна Кольору Користувача

```bash
# Користувач Діана змінює колір
/edituser diana - - #FF6B9D
```

**Результат:**
```
✅ Користувача оновлено:
Позиція: 0
Ім'я: Діана (diana)
Колір: #FF6B9D
Статус: ✅ Активний
```

### Сценарій 2: Тимчасове Видалення Користувача

```bash
# Женя в відпустці на місяць
/removeuser zhenya
```

**Результат:**
- Женя не відображається в новому розкладі
- Старі призначення зберігаються в історії
- Можна швидко активувати назад

```bash
# Через місяць - повернення
/activateuser zhenya
```

### Сценарій 3: Зміна Імені Користувача

```bash
# Користувач змінив прізвище
/edituser 1 Даша dasha -
```

### Сценарій 4: Перегляд Всіх Користувачів

```bash
/users
```

## 🔒 Безпека

- ✅ Всі команди доступні **тільки адміністраторам**
- ✅ Перевірка наявності користувача перед операцією
- ✅ Валідація всіх вхідних параметрів
- ✅ Детальне логування всіх операцій
- ✅ Збереження історії при деактивації

## ⚠️ Важливо

### Різниця між `/removeuser` та `/adduser`

- **`/removeuser`** - деактивує користувача (м'яке видалення)
  - `is_active = False`
  - Дані зберігаються
  - Легко відновити

- **`/adduser`** - створює або **повністю перезаписує** користувача
  - Може змінити статус активності
  - Оновлює всі поля

### Рекомендації

1. **Для тимчасового видалення** → використовуйте `/removeuser`
2. **Для швидких змін** → використовуйте `/edituser`
3. **Для створення нового** → використовуйте `/adduser`

## 🧪 Тестування

### Тест-кейси

1. ✅ Редагування існуючого користувача
2. ✅ Редагування з використанням тире
3. ✅ Деактивація активного користувача
4. ✅ Спроба деактивувати вже деактивованого
5. ✅ Активація деактивованого користувача
6. ✅ Спроба активувати вже активного
7. ✅ Пошук користувача за позицією
8. ✅ Пошук користувача за ім'ям
9. ✅ Валідація hex кольору
10. ✅ Обробка неіснуючого користувача

### Ручне Тестування

```bash
# 1. Створити тестового користувача
/adduser 3 Тест test #AABBCC

# 2. Редагувати ім'я
/edituser test Тестовий - -

# 3. Змінити колір
/edituser 3 - - #112233

# 4. Деактивувати
/removeuser test

# 5. Перевірити список
/users

# 6. Активувати назад
/activateuser test

# 7. Видалити (встановити неактивним назавжди)
/removeuser 3
```

## 📊 Статистика Змін

### Файли Змінено

- **`src/bot.py`**: +243 рядки
  - 3 нові команди
  - Оновлена довідка
  - Оновлені command hints

### Додані Команди

| Команда | Призначення | Рядки коду |
|---------|-------------|------------|
| `/edituser` | Редагування користувача | ~70 |
| `/removeuser` | Деактивація користувача | ~61 |
| `/activateuser` | Активація користувача | ~61 |

## 🚀 Майбутні Покращення

### Можливі Доповнення

1. **Інтерактивний режим**
   - Callback-кнопки для вибору користувача
   - Форма редагування з inline клавіатурою

2. **Масові операції**
   - Деактивувати декількох користувачів одночасно
   - Експорт/імпорт конфігурації користувачів

3. **Історія змін**
   - Логування змін параметрів користувачів
   - Відкат до попереднього стану

4. **Розширена валідація**
   - Перевірка унікальності імен
   - Попередження при конфлікті кольорів

## 📚 Пов'язана Документація

- [`ADMIN_GUIDE.md`](./ADMIN_GUIDE.md) - Повний посібник адміністратора
- [`ADMIN_QUICKSTART.md`](./ADMIN_QUICKSTART.md) - Швидкий старт
- [`PROJECT_SUMMARY.md`](./PROJECT_SUMMARY.md) - Огляд проекту
- [`DEVELOPMENT.md`](./DEVELOPMENT.md) - Розробка та архітектура

## 👥 Автор

Реалізовано: GitHub Copilot  
Дата: 2025-10-11  
Версія: 1.0

---

**Примітка**: Всі команди доступні тільки адміністраторам і логуються для аудиту.
